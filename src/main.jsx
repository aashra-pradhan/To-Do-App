import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
//App.jsx is in the same folder, import garda ./ garnuparcha, yedi component bhako file same folder ma cha bhane
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
